package array_practice;
import java.util.*;

public class SplitArrayInSize {
  /*  public static void main(String[] args){
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[] arr = new int[n];
        for(int i=0; i<n; i++){
            arr[i] = sc.nextInt();
        }
        int spliSize = sc.nextInt();

        List<int[]> ans = splitArray(arr, spliSize);
        //ans.forEach(spliArray -> System.out.println(Arrays.toString(spliArray)));
    }
    public static List<int[]> splitArray(int[] array, int splitSize){
        ArrayList<int[]> res = new ArrayList<int[]>();

        int k = splitSize;
        int i=0, n= array.length;
        while(i < n){
            int j=0;
            int[] temp = new int[k];
            while( j < k && i <n){
                temp[j] = array[i];
                i++;
                j++;
            }
            res.add(temp);
        }
        return res;

    }*/
}
